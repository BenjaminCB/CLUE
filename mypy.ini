[mypy]

# sympy ships no py.typed marker, so mypy cannot analyse it. Domain and friends
# degrade to Any, which is enough for the way this codebase uses them.
[mypy-sympy.*]
ignore_missing_imports = True

# scipy is typed only via the separate scipy-stubs distribution.
[mypy-scipy.*]
ignore_missing_imports = True
